{
  "name": "actions-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test:pdv": "playwright test --config playwright.pdv.config.ts"
  },
  "dependencies": {
    "@asymmetric-effort/specifyjs": "^0.1.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "typescript": "^6.0.3",
    "vite": "^5.4.0"
  }
}
